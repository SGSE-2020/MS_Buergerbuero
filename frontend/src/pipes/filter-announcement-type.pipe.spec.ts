import { FilterAnnouncementTypePipe } from './filter-announcement-type.pipe';

describe('FilterAnnouncementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnouncementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
