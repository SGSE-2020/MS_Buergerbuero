import { FilterAnnouncementUserPipe } from './filter-announcement-user.pipe';

describe('FilterAnnouncementUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnouncementUserPipe();
    expect(pipe).toBeTruthy();
  });
});
